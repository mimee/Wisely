<RelativeLayout android:id="@+id/activityRowLayout"
	android:layout_width="fill_parent" android:layout_height="fill_parent"
	xmlns:android="http://schemas.android.com/apk/res/android" android:baselineAligned="false">

	<TextView android:text="activity" android:id="@+id/activityRow"
		android:layout_width="wrap_content" android:layout_height="wrap_content">
	</TextView>
	<Button android:text="Start" android:id="@+id/startActivityBtn"
		android:layout_width="wrap_content" android:layout_height="wrap_content"
		android:onClick="startActivityHandler" android:layout_toLeftOf="@+id/editActivityBtn">
	</Button>
	<Button android:text="Edit" android:id="@+id/editActivityBtn"
		android:layout_width="wrap_content" android:layout_height="wrap_content"
		android:onClick="editActivityHandler" android:layout_toLeftOf="@+id/deleteActivityBtn">
	</Button>
	<Button android:text="Delete" android:id="@+id/deleteActivityBtn"
		android:layout_width="wrap_content" android:layout_height="wrap_content"
		android:onClick="deleteActivityHandler" android:layout_alignParentRight="true">
	</Button>

</RelativeLayout>
